{
  "name": "nba-higher-lower",
  "version": "0.1.0",
  "description": "NBA higher-or-lower stat duel",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "fetch": "node scripts/fetch-players.mjs",
    "fetch:meta": "node scripts/fetch-meta.mjs"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
